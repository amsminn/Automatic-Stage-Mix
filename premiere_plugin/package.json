{
    "name": "automatic-stage-mix",
    "version": "1.0.0",
    "scripts": {
        "watch-host": "cd host_src && npm start",
        "start": "copy-and-watch --watch \"{client|CSXS|host}\" ./dist",
        "build": "cd host_src && npm run build && cd .. && copy-and-watch ./ ./dist",
        "lint": "eslint --ext .ts,.tsx . --fix --cache"
    },
    "author": "noname0310",
    "license": "MIT",
    "sideEffects": false,
    "devDependencies": {
        "copy-and-watch": "^0.1.6"
    }
}
